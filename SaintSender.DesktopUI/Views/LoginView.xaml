<Window x:Class="SaintSender.DesktopUI.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SaintSender.DesktopUI.Views"
        mc:Ignorable="d"
        Title="Login" Height="190" Width="359">
    <StackPanel Margin="10">
        <Label Content="_Email:" Target="{Binding ElementName=txtEmail}" />
        <TextBox Name="txtEmail" Width="140" HorizontalAlignment="Left" Text="{Binding TextBoxEmailInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Content="_Password:" Target="{Binding ElementName=txtPassword}" Margin="5,0,-5,0" />
        <PasswordBox Name="txtPassword" Width="140" HorizontalAlignment="Left" Password=""   />
        <UniformGrid Columns="2">
            <TextBlock Name="txtInfo" Foreground="{Binding TextColor}" Text="{Binding TextInformation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  Margin="10" HorizontalAlignment="Left"></TextBlock>
            <Button Margin="10" Width="50"  HorizontalAlignment="Right" Command="{Binding ButtonSignInClick}" CommandParameter="{Binding ElementName=txtPassword}"  >
                Sign In
            </Button>
        </UniformGrid>
    </StackPanel>
</Window>
